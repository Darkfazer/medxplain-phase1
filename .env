# MedXplain Environment Configuration
# Copy this file to .env and fill in your API keys.
# The backend loads these automatically if you use python-dotenv or export them manually.

# ── LLM Query Reformulation (Gemini 1.5 Flash Free Tier) ──
# Get a free API key at: https://aistudio.google.com/app/apikey
# Without this key, reformulation is disabled and the original question is used.
GEMINI_API_KEY=AIzaSyAFtmrw_Arqo_IAZ51qfbegdoyrUqO4b7w

# ── Device Configuration ──
# MEDXPLAIN_DEVICE=cuda       # cuda | cpu
# MEDXPLAIN_REQUIRE_CUDA=1    # 1 = crash if no GPU, 0 = fallback to CPU